version: "3.9"

services:
  database:
    image: postgres:13.3
    restart: always
    environment:
      POSTGRES_USER: "test"
      POSTGRES_PASSWORD: "test"
      POSTGRES_DB: "test"
    ports:
      - "5432:5432"
    volumes:
      - db:/data/postgres

volumes:
  db: